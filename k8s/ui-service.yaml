apiVersion: v1
kind: Service
metadata:
  name: task-ui-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: task-ui
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001
    protocol: TCP